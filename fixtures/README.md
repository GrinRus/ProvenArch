# Fixtures (planned)

This folder will hold minimal fixture workspaces and golden outputs to prevent regressions.

Planned:
- fixture repo(s) with a small service + infra
- golden outputs under `fixtures/<name>/golden/`

name: live-runner-smoke

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  live-runner-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.20.x"

      - name: Optional live runner smoke
        run: |
          echo "Optional manual job. Configure claude-code runner in this environment before enabling real smoke checks."

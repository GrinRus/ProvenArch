# Legacy compatibility slice. Keep for ad-hoc diagnostics; canonical non-release profiles live in
# examples/e2e-profile-catalog.yaml and examples/e2e-matrix.regres-*.yaml.
version: 1
profiles:
  - id: single-path
    source_kind: path
    expected_repo_count: 1
    repos_file: ./repos/curated/single-path.posthog.repos.yaml

  - id: multi-git_url
    source_kind: git_url
    expected_repo_count: 5
    repos_file: ./repos/github/multi-sentry-ecosystem.repos.yaml

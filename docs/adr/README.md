# ADRs

We record architectural decisions as short ADRs.

## When to write an ADR
Write an ADR when you:
- pick an implementation stack
- change schemas/contracts
- introduce a major dependency
- change persistence or pipeline semantics

## Naming
`ADR-YYYYMMDD-<slug>.md`

## Template
Use `0000-template.md`.

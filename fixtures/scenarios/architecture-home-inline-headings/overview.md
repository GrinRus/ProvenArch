# Architecture Home

## System at a glance The application exposes customer-facing banking services described by `README.md`.

## Analyzed scope The analyzed repository scope and entrypoints are documented by `README.md`.

## Domains and ownership Domain responsibilities are recorded in the canonical model under `model/entities/`.

## Key flows User and service flows are described by `reports/as-is/overview.md`.

## Integrations and datastores Integration and persistence evidence is recorded in `model/entities/`.

## Where to start Begin with `README.md` and the coverage summary at `reports/coverage/summary.md`.

## Safe-change guidance Validate changes against the cited repository paths and affected domain evidence.

## Evidence gaps and open questions Unconfirmed details remain explicit gaps in `reports/coverage/summary.md`.
